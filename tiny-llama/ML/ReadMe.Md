

Install ollama

Run `ollama serve` in bash

Run `ollama run tinyllama` once

create a fresh venv
`python3 -m venv venv`

activate
`source venv/bin/activate`

ingest data
`python3 ingest.py`

run query
`python3 query.py`